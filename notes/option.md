# option

- `get`, `getx`
- `map`, `map2`, ...
- `flatMap`, (does `flatMap2` make sense?)
- `forEach`
- `isNone`, `isSome`, `has` (?)
- `makeSome`, `makeNone`
- `equals` (or equals inner?)
- `compare` (or compare inner?)
