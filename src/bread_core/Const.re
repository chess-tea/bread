let compareDesc =
  "compares $1 and $2. A negative integer is returned when $1 < $2, a positive "
  ++ "integer is returned when $1 > $2, and 0 is returned when $1 equals $2";

let equalDesc = "tests for equality between $1 and $2";
